# Maykelsgym Shop

Panel de control del negocio de Maykel's Gym (La Habana): matrículas, nevera, vitrina, mensajería, inventario y ganancias.

Desplegado en Vercel (build de Vite, ver `vercel.json`). Requiere las variables de entorno
`VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY`.
